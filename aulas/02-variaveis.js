// estrutura de uma variavel
// var (var nome_variavel = <valor>;)

var nome = "Caio Tirado";
var idade = 16;

// exibindo nome no console.
//console.log("Meu nome é:", nome);

// como executar: botão direito na pasta aulas
// Abrir terminal integrado -> node + nome do arquivo
// Exemplo: node 02-variaveis.js

console.log("meu nome é:", nome + ", minha idade é:", idade + "\n");

// JavaScript utiliza case sensitive (maíusculas são diferentes de minúsculas)
// 

// Regra das variáveis
// Podem iniciar com "A-Z", "a-z", "_"

// Regra das variáveis
//podem iniciar com "A-Z", "a-z", "_" ou "$"
//Não pode ter em uma variável (ç, á, ã, à, â)
let MinhaVariavel = 10;
let outraVariavel = 2.65;
const _minhaConstante = "UmaConstante!";
var $minhaVariavel = -5;

// Variáveis lógicas (boolean)
var Nome = "seu nomeeeeeee";
var rico = false;
var eProfessor = true;

console.log("Nome: ", Nome, "| é profesor?", eProfessor, "| é rico?", rico);